numpy
scipy
matplotlib
pandas

mne
mne-bids

joblib
parse

pyyaml==5.3.1
pyyaml-include==1.2

tqdm

# install torch separately first with the right CUDA/CPU build for your machine:
#   GPU (CUDA 12.6): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#   CPU only:        pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# graph neural network support — scatter/sparse not needed, PyG 2.x uses native torch ops
torch-geometric

# moabb<1.0 avoids np.int removal error with newer numpy (patched in neiry.py if already installed)
moabb<1.0

# EEG/BIDS spatial processing
scikit-learn

# task switch detection and label generation
datalad

# suppress urllib3/requests version mismatch noise
urllib3<2
charset-normalizer<3
